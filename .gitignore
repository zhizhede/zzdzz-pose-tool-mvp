# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/
# Python 构建产物（仅根目录；webui/dist 是前端构建产物，需要入库）
/dist/
build/

# 本地私密配置（API 密钥等），永不入库
config.local.yaml

# 批量渲染产物（姿态库里的 preview.png 才是提交对象）
renders/

# 前端依赖（构建产物 webui/dist 提交入库，Python 用户免 Node）
webui/node_modules/

# 编辑器
.vscode/
.idea/
